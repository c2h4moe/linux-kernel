cxl_pool.c定义了一个内核模块，用于管理CXL共享内存。该模块将CXL内存抽象为/dev/cxl_pool，用户空间程序通过ioctl分配和释放CXL内存资源（使用例可见test/cxl_pool_test.c）。
编译内核时需要配置以m模式编译cxl_pool模块，编译内核后可通过make modules_install安装模块。安装完成后，进入qemu环境进行测试。
进入qemu后，首先运行指令
cxl create-region -m -d decoder0.0 -w 1 mem0 -s 512Mte-namespace -m dax -r region0
ndctl create-namespace -m dax -r region0
然后加载cxl_pool模块，运行指令
insmod /lib/modules/6.8.0+/kernel/drivers/cxl/pool/cxl_pool.ko.zst
最后可以运行测试程序。
runtime/目录下有一个精简版用户空间运行时原型：
- 单个内核模块实例管理整个CXL pool
- “多主机”由多个用户态进程模拟
- 只支持一个宏进程
- 不包含heartbeat、membership和容错逻辑
